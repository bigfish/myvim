#!/bin/bash
PROJECT_DIR=PROJECTDIR
NS_PATH=NAMESPACE_PATH
${PROJECT_DIR}/bin/APP_NAME.swf:	${PROJECT_DIR}/src/${NS_PATH}/APP_NAME.hx
	haxe ${PROJECT_DIR}/build.hxml
	${FLASHPLAYER} "${PROJECT_DIR}/bin/APP_NAME.swf"
